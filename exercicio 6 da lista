#include<conio.h>
#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<locale.h>

int main ()
{
	int a,b,c;
	printf("informe a \n");
	scanf ("%i", &a);
	printf("informe b \n");
	scanf ("%i", &b);
	printf("informe c \n");
	scanf ("%i", &c);
	
	if ((a<b) && (a<c) && (b<c))
	{
		printf ("%i %i %i",a,b,c);
	}
		else if ((a<b) && (a<c) && (c<b))
		{
			printf("%i %i %i",a,c,b);
		}
		
			else if ((b<a) && (b<c) && (c<a)){
				printf("%i %i %i",a,c,b);
			}
			
				else if ((c<a) && (c<b) && (a<b)){
					printf("%i %i %i",c,a,b);
				}
					else
					{
						printf("%i %i %i",c,b,a);
					}
}
